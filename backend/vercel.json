{
  "installCommand": "cd .. && npm install --include=dev",
  "buildCommand": "cd .. && npm run build -w backend",
  "outputDirectory": ".",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
